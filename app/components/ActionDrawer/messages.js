/*
 * ActionDrawer Messages
 *
 * This contains all the text for the ActionDrawer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ActionDrawer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ActionDrawer component!',
  },
});
