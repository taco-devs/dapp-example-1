/*
 * MintModal Messages
 *
 * This contains all the text for the MintModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MintModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MintModal component!',
  },
});
