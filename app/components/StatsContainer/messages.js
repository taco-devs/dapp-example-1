/*
 * StatsContainer Messages
 *
 * This contains all the text for the StatsContainer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StatsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatsContainer component!',
  },
});
