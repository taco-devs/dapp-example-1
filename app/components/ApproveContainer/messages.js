/*
 * ApproveContainer Messages
 *
 * This contains all the text for the ApproveContainer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ApproveContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApproveContainer component!',
  },
});
