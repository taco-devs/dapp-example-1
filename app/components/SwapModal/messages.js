/*
 * SwapModal Messages
 *
 * This contains all the text for the SwapModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SwapModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SwapModal component!',
  },
});
