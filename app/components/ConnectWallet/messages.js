/*
 * ConnectWallet Messages
 *
 * This contains all the text for the ConnectWallet component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ConnectWallet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Connect Wallet',
  },
});
