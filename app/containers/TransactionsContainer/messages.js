/*
 * TransactionsContainer Messages
 *
 * This contains all the text for the TransactionsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransactionsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionsContainer container!',
  },
});
