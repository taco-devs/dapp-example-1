/*
 * SwapContainer Messages
 *
 * This contains all the text for the SwapContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SwapContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SwapContainer container!',
  },
});
