/*
 * GrowthStats Messages
 *
 * This contains all the text for the GrowthStats container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GrowthStats';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GrowthStats container!',
  },
});
