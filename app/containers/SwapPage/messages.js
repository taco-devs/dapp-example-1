/*
 * SwapPage Messages
 *
 * This contains all the text for the SwapPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SwapPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SwapPage container!',
  },
});
